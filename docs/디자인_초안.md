좋습니다. **Tailwind + shadcn/ui + TanStack Table**로 지금 UI를 “Windows 11/Fluent 2 감성”에 맞게 현대화하는 **Vite(React)용 스킨 + 샘플 코드 패키지**를 아래에 정리했습니다.
모든 예시는 **Vite + Tauri(Windows 11)** 기준이며, shadcn/ui의 **Vite 가이드/다크모드 가이드/테마 토큰**, TanStack Table의 **가상 스크롤 가이드**, Radix의 **접근성 프리미티브**, Lucide 아이콘 문서에 맞춰 작성했습니다. ([Shadcn UI][1], [TanStack][2], [radix-ui.com][3], [Lucide][4])

---

## 1) 설치 & 초기 구성

> 이미 Vite + React + Tauri 프로젝트라면 **설치/추가**만 진행하면 됩니다.

### 1-1. Tailwind + shadcn/ui + 아이콘

```bash
# Tailwind(v4, Vite 플러그인)
npm i tailwindcss @tailwindcss/vite

# shadcn/ui 초기화 (Vite 공식 가이드)
npx shadcn@latest init

# 자주 쓸 컴포넌트 일괄 추가
npx shadcn@latest add button menubar sidebar table scroll-area resizable \
  progress dropdown-menu input select label switch textarea sonner \
  separator card badge tabs tooltip

# TanStack Table + Virtual
npm i @tanstack/react-table @tanstack/react-virtual

# 아이콘
npm i lucide-react
```

* shadcn/ui의 **Vite 설치 가이드**에서는 `@tailwindcss/vite` 플러그인을 Vite 설정에 추가하고, `src/index.css`에 `@import "tailwindcss";`만 두는 구성을 권장합니다. 아래에서 그대로 반영합니다. ([Shadcn UI][1])
* 다크 모드는 shadcn/ui **Vite 다크 모드 가이드**의 `ThemeProvider` 코드를 사용해 **system/light/dark** 3-way 토글을 제공합니다. ([Shadcn UI][5])
* Lucide 아이콘은 **React 컴포넌트 형태로 트리 셰이킹**되어 필요한 아이콘만 번들됩니다. ([Lucide][4], [npm][6])

### 1-2. Vite/TS 설정

**`vite.config.ts`**

```ts
import path from "path";
import tailwindcss from "@tailwindcss/vite";
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: { "@": path.resolve(__dirname, "./src") },
  },
});
```

Vite + shadcn/ui 문서에서 **경로 별칭(@)** 과 **Tailwind 플러그인** 추가를 안내합니다. ([Shadcn UI][1])

**`tsconfig.json` / `tsconfig.app.json`** (경로 별칭)

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  }
}
```

([Shadcn UI][1])

---

## 2) Tailwind & 테마 토큰(Fluent 2 톤)

**`src/index.css`**

```css
@import "tailwindcss";

/* Tailwind v4: class 기반 다크 모드 토글을 위한 사용자 정의 variant */
@custom-variant dark (&:where(.dark, .dark *));

/* shadcn/ui 권장 CSS 변수 테마 (OKLCH 기반) */
@layer base {
  :root {
    --radius: .625rem;
    --background: oklch(1 0 0);
    --foreground: oklch(0.145 0 0);
    --card: oklch(1 0 0);
    --card-foreground: oklch(0.145 0 0);
    --popover: oklch(1 0 0);
    --popover-foreground: oklch(0.145 0 0);
    --primary: oklch(0.205 0 0);
    --primary-foreground: oklch(0.985 0 0);
    --secondary: oklch(0.97 0 0);
    --secondary-foreground: oklch(0.205 0 0);
    --muted: oklch(0.97 0 0);
    --muted-foreground: oklch(0.556 0 0);
    --accent: oklch(0.97 0 0);
    --accent-foreground: oklch(0.205 0 0);
    --destructive: oklch(0.577 0.245 27.325);
    --border: oklch(0.922 0 0);
    --input: oklch(0.922 0 0);
    --ring: oklch(0.708 0 0);

    /* Sidebar 전용 토큰 (shadcn/sidebar) */
    --sidebar: oklch(0.985 0 0);
    --sidebar-foreground: oklch(0.145 0 0);
    --sidebar-primary: oklch(0.205 0 0);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.97 0 0);
    --sidebar-accent-foreground: oklch(0.205 0 0);
    --sidebar-border: oklch(0.922 0 0);
    --sidebar-ring: oklch(0.708 0 0);
  }

  .dark {
    --background: oklch(0.145 0 0);
    --foreground: oklch(0.985 0 0);
    --card: oklch(0.205 0 0);
    --card-foreground: oklch(0.985 0 0);
    --popover: oklch(0.269 0 0);
    --popover-foreground: oklch(0.985 0 0);
    --primary: oklch(0.922 0 0);
    --primary-foreground: oklch(0.205 0 0);
    --secondary: oklch(0.269 0 0);
    --secondary-foreground: oklch(0.985 0 0);
    --muted: oklch(0.269 0 0);
    --muted-foreground: oklch(0.708 0 0);
    --accent: oklch(0.371 0 0);
    --accent-foreground: oklch(0.985 0 0);
    --destructive: oklch(0.704 0.191 22.216);
    --border: oklch(1 0 0 / 10%);
    --input: oklch(1 0 0 / 15%);
    --ring: oklch(0.556 0 0);

    --sidebar: oklch(0.205 0 0);
    --sidebar-foreground: oklch(0.985 0 0);
    --sidebar-primary: oklch(0.488 0.243 264.376);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.269 0 0);
    --sidebar-accent-foreground: oklch(0.985 0 0);
    --sidebar-border: oklch(1 0 0 / 10%);
    --sidebar-ring: oklch(0.439 0 0);
  }

  * { @apply border-border; }
  body { @apply bg-background text-foreground; }
}
```

* 위 토큰은 shadcn/ui **Theming** 문서의 권장 변수 이름을 그대로 사용합니다. 컴포넌트 전체가 `bg-background`, `text-foreground`, `bg-primary` 같은 유틸로 일관되게 스타일됩니다. ([Shadcn UI][7])
* 다크 모드는 Tailwind **Dark mode(Core concepts)** 방식으로 `dark` 변형을 클래스로 토글하도록 설정했습니다. ([Tailwind CSS][8])
* `sidebar` 변수는 shadcn/ui **Sidebar** 컴포넌트가 요구하는 색 토큰과 일치합니다. ([Shadcn UI][9])

---

## 3) 다크 모드: ThemeProvider & 토글

**`src/components/theme-provider.tsx`**
(shadcn/ui **Vite 다크 모드** 가이드 그대로)

```tsx
import { createContext, useContext, useEffect, useState } from "react";

type Theme = "dark" | "light" | "system";
type ThemeProviderProps = {
  children: React.ReactNode;
  defaultTheme?: Theme;
  storageKey?: string;
};
type ThemeProviderState = {
  theme: Theme;
  setTheme: (theme: Theme) => void;
};

const ThemeProviderContext = createContext<ThemeProviderState>({
  theme: "system",
  setTheme: () => null,
});

export function ThemeProvider({
  children,
  defaultTheme = "system",
  storageKey = "vite-ui-theme",
  ...props
}: ThemeProviderProps) {
  const [theme, setTheme] = useState<Theme>(
    () => (localStorage.getItem(storageKey) as Theme) || defaultTheme
  );

  useEffect(() => {
    const root = window.document.documentElement;
    root.classList.remove("light", "dark");
    if (theme === "system") {
      const systemTheme = window.matchMedia("(prefers-color-scheme: dark)").matches
        ? "dark"
        : "light";
      root.classList.add(systemTheme);
      return;
    }
    root.classList.add(theme);
  }, [theme]);

  const value = {
    theme,
    setTheme: (t: Theme) => { localStorage.setItem(storageKey, t); setTheme(t); }
  };

  return (
    <ThemeProviderContext.Provider {...props} value={value}>
      {children}
    </ThemeProviderContext.Provider>
  );
}

export const useTheme = () => {
  const context = useContext(ThemeProviderContext);
  if (context === undefined) throw new Error("useTheme must be used within a ThemeProvider");
  return context;
};
```

**`src/components/mode-toggle.tsx`**

```tsx
import { Moon, Sun } from "lucide-react";
import { Button } from "@/components/ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { useTheme } from "@/components/theme-provider";

export function ModeToggle() {
  const { setTheme } = useTheme();
  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="outline" size="icon" aria-label="Toggle theme">
          <Sun className="h-4 w-4 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
          <Moon className="absolute h-4 w-4 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem onClick={() => setTheme("light")}>Light</DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("dark")}>Dark</DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("system")}>System</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

위 구현은 shadcn/ui **Vite용 다크 모드 문서**의 권장 구조입니다. ([Shadcn UI][5])

---

## 4) 앱 셸: 좌측 **Sidebar** + 상단 **Menubar** + 중앙 가상 테이블 + 우측 Inspector

> shadcn/ui **Sidebar**는 접이식(icons-only)까지 포함한 고급 사이드바 컴포넌트이며, **Menubar**는 데스크톱 앱 스타일의 상단 커맨드 메뉴에 적합합니다. 스크롤/리사이즈는 **Scroll-area, Resizable**로 유연하게 만듭니다. ([Shadcn UI][9])

**`src/App.tsx`**

```tsx
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "@/components/ui/sonner";
import { SidebarProvider, SidebarInset } from "@/components/ui/sidebar";
import { AppSidebar } from "@/components/app/app-sidebar";
import { TopMenubar } from "@/components/app/top-menubar";
import { ResizableHandle, ResizablePanel, ResizablePanelGroup } from "@/components/ui/resizable";
import { LeftPanel } from "@/components/app/left-panel";
import { InspectorPanel } from "@/components/app/inspector-panel";
import { DataTableVirt } from "@/components/data-table/data-table-virt";

export default function App() {
  return (
    <ThemeProvider defaultTheme="system" storageKey="vite-ui-theme">
      <SidebarProvider>
        <AppSidebar />
        <SidebarInset>
          <TopMenubar />
          <div className="h-[calc(100vh-40px)] p-3">
            <ResizablePanelGroup direction="horizontal" className="rounded-lg border">
              <ResizablePanel defaultSize={24} minSize={18}>
                <LeftPanel />
              </ResizablePanel>
              <ResizableHandle withHandle />
              <ResizablePanel defaultSize={52} minSize={36}>
                <DataTableVirt />
              </ResizablePanel>
              <ResizableHandle withHandle />
              <ResizablePanel defaultSize={24} minSize={18}>
                <InspectorPanel />
              </ResizablePanel>
            </ResizablePanelGroup>
          </div>
        </SidebarInset>
      </SidebarProvider>
      <Toaster />
    </ThemeProvider>
  );
}
```

### 4-1. Sidebar (왼쪽 네비)

**`src/components/app/app-sidebar.tsx`**

```tsx
import {
  Sidebar, SidebarContent, SidebarHeader, SidebarFooter,
  SidebarGroup, SidebarGroupLabel, SidebarGroupContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton
} from "@/components/ui/sidebar";
import { Home, Settings, Book, WandSparkles, ListChecks } from "lucide-react";

const items = [
  { title: "프로젝트", icon: Home },
  { title: "설정", icon: Settings },
  { title: "용어집", icon: Book },
  { title: "커스텀 프롬프트", icon: WandSparkles },
  { title: "로그", icon: ListChecks },
];

export function AppSidebar() {
  return (
    <Sidebar collapsible="icon">
      <SidebarHeader />
      <SidebarContent>
        <SidebarGroup>
          <SidebarGroupLabel>메뉴</SidebarGroupLabel>
          <SidebarGroupContent>
            <SidebarMenu>
              {items.map((it) => (
                <SidebarMenuItem key={it.title}>
                  <SidebarMenuButton asChild>
                    <button type="button" title={it.title} className="w-full justify-start">
                      <it.icon className="mr-2 h-4 w-4" />
                      <span>{it.title}</span>
                    </button>
                  </SidebarMenuButton>
                </SidebarMenuItem>
              ))}
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>
      <SidebarFooter />
    </Sidebar>
  );
}
```

* Sidebar는 색 토큰(`--sidebar-*`)을 사용하며, **아이콘 전용 축소 모드**도 지원합니다. ([Shadcn UI][9])

### 4-2. Menubar (상단 커맨드)

**`src/components/app/top-menubar.tsx`**

```tsx
import { Menubar, MenubarMenu, MenubarTrigger, MenubarContent, MenubarItem, MenubarSeparator } from "@/components/ui/menubar";
import { ModeToggle } from "@/components/mode-toggle";

export function TopMenubar() {
  return (
    <div className="sticky top-0 z-50 border-b bg-background">
      <div className="mx-auto flex h-10 items-center justify-between px-2">
        <Menubar className="border-none shadow-none">
          <MenubarMenu>
            <MenubarTrigger>파일</MenubarTrigger>
            <MenubarContent>
              <MenubarItem>API 키 설정</MenubarItem>
              <MenubarItem>열기...</MenubarItem>
              <MenubarItem>저장</MenubarItem>
              <MenubarSeparator />
              <MenubarItem>내보내기</MenubarItem>
            </MenubarContent>
          </MenubarMenu>
          <MenubarMenu>
            <MenubarTrigger>번역</MenubarTrigger>
            <MenubarContent>
              <MenubarItem>검증 실행</MenubarItem>
              <MenubarItem>번역 시작</MenubarItem>
              <MenubarItem>취소</MenubarItem>
            </MenubarContent>
          </MenubarMenu>
        </Menubar>
        <ModeToggle />
      </div>
    </div>
  );
}
```

* Menubar는 데스크톱 앱의 **영구 메뉴** UI 패턴을 제공합니다. ([Shadcn UI][10])

---

## 5) 왼쪽 패널(설정/필터/프롬프트/용어집/로그)

**`src/components/app/left-panel.tsx`**

```tsx
import { ScrollArea } from "@/components/ui/scroll-area";
import { Card } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Input } from "@/components/ui/input";
import { Select, SelectTrigger, SelectContent, SelectItem, SelectValue } from "@/components/ui/select";
import { Switch } from "@/components/ui/switch";
import { Textarea } from "@/components/ui/textarea";
import { Button } from "@/components/ui/button";
import { Separator } from "@/components/ui/separator";
import { Badge } from "@/components/ui/badge";

export function LeftPanel() {
  return (
    <ScrollArea className="h-full">
      <div className="flex flex-col gap-3 p-3">
        <Card className="p-3">
          <div className="mb-2 text-sm font-semibold">필터</div>
          <div className="flex flex-wrap gap-2">
            <Badge variant="secondary">전체 0</Badge>
            <Badge className="bg-primary/10 text-primary hover:bg-primary/20">미번역 0</Badge>
            <Badge variant="secondary">완료 0</Badge>
            <Badge variant="secondary">오류 0</Badge>
          </div>
        </Card>

        <Card className="space-y-3 p-3">
          <div className="text-sm font-semibold">처리 설정</div>
          <div className="grid grid-cols-2 gap-3">
            <div>
              <Label>동시성</Label>
              <Input placeholder="10" />
            </div>
            <div>
              <Label>배치 크기</Label>
              <Input placeholder="8" />
            </div>
            <div className="col-span-2">
              <Label>모델</Label>
              <Select defaultValue="gemini-1.5-flash">
                <SelectTrigger><SelectValue /></SelectTrigger>
                <SelectContent>
                  <SelectItem value="gemini-1.5-flash">gemini-1.5-flash</SelectItem>
                  <SelectItem value="gemini-2.0-flash">gemini-2.0-flash</SelectItem>
                </SelectContent>
              </Select>
            </div>
            <div className="col-span-2 flex items-center justify-between rounded-md border p-2">
              <div className="text-sm">민감 로그 차단</div>
              <Switch />
            </div>
            <div className="col-span-2 flex gap-2">
              <Button size="sm">설정 저장</Button>
              <Button size="sm" variant="secondary">오류 내보내기</Button>
            </div>
          </div>
        </Card>

        <Card className="space-y-2 p-3">
          <div className="text-sm font-semibold">커스텀 프롬프트</div>
          <Textarea rows={5} placeholder="추가 지침을 입력하세요" />
        </Card>

        <Card className="space-y-2 p-3">
          <div className="text-sm font-semibold">용어집(JSON)</div>
          <Textarea rows={8} placeholder='[{"key":"Dragonborn","value":"드래곤본"}]' />
          <div className="flex gap-2">
            <Button size="sm">용어집 저장</Button>
            <Button size="sm" variant="secondary">초기화</Button>
          </div>
        </Card>

        <Card className="p-3">
          <div className="mb-2 text-sm font-semibold">로그</div>
          <div className="text-xs text-muted-foreground">로그 없음</div>
        </Card>

        <Separator />
        <div className="pb-4" />
      </div>
    </ScrollArea>
  );
}
```

* 스크롤 가능한 패널은 shadcn/ui **Scroll-area**로 크로스브라우저 스크롤바/모바일 스크롤 터치 품질을 개선합니다. ([Shadcn UI][11])

---

## 6) 중앙 그리드: **TanStack Table + @tanstack/react-virtual** (수만 행도 부드럽게)

> TanStack Table은 **헤드리스**라 원하는 마크업/스타일을 그대로 적용할 수 있고, 가상화는 **TanStack Virtual** 또는 `react-window`와 쉽게 연동합니다. 아래 구현은 **고정 행 높이 가상화**로 충분히 빠르고, 동적 높이가 필요하면 문서 예시를 참고해 `measureElement`를 추가하세요. ([Shadcn UI][12], [TanStack][13])

**`src/components/data-table/columns.tsx`**

```tsx
import { ColumnDef } from "@tanstack/react-table";

export type Row = {
  id: string;
  status: "미번역" | "완료" | "오류";
  source: string;   // 원문
  target: string;   // 번역문
};

export const columns: ColumnDef<Row>[] = [
  { accessorKey: "id", header: "ID", size: 96 },
  { accessorKey: "status", header: "상태", size: 120 },
  { accessorKey: "source", header: "원문" },
  { accessorKey: "target", header: "번역" },
];
```

**`src/components/data-table/data-table-virt.tsx`**

```tsx
import * as React from "react";
import { useReactTable, getCoreRowModel, flexRender } from "@tanstack/react-table";
import { useVirtualizer } from "@tanstack/react-virtual";
import { columns, type Row } from "./columns";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Card } from "@/components/ui/card";

function makeRows(n = 50000): Row[] {
  return Array.from({ length: n }, (_, i) => ({
    id: String(i + 1),
    status: i % 11 === 0 ? "오류" : i % 7 === 0 ? "완료" : "미번역",
    source: `This is a sample source text #${i + 1}`,
    target: "",
  }));
}

export function DataTableVirt() {
  const [data] = React.useState<Row[]>(() => makeRows(30000));

  const table = useReactTable({
    data, columns, getCoreRowModel: getCoreRowModel(),
    // 여기서 정렬/필터/편집 등 필요한 기능들을 점진적으로 추가
  });

  const parentRef = React.useRef<HTMLDivElement>(null);
  const rows = table.getRowModel().rows;

  const rowVirtualizer = useVirtualizer({
    count: rows.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 44,     // 행 높이 추정치
    overscan: 8,                // 성능·부드러움 트레이드오프
  });

  return (
    <Card className="h-full overflow-hidden">
      <div ref={parentRef} className="h-full overflow-auto">
        <Table className="relative">
          {/* 헤더 고정 */}
          <TableHeader className="sticky top-0 z-10 bg-background">
            {table.getHeaderGroups().map((hg) => (
              <TableRow key={hg.id}>
                {hg.headers.map((h) => (
                  <TableHead key={h.id} style={{ width: h.getSize() }}>
                    {h.isPlaceholder ? null : flexRender(h.column.columnDef.header, h.getContext())}
                  </TableHead>
                ))}
              </TableRow>
            ))}
          </TableHeader>

          {/* 가상화된 바디 (grid + absolute) */}
          <TableBody
            style={{
              display: "grid",
              height: `${rowVirtualizer.getTotalSize()}px`,
              position: "relative",
            }}
          >
            {rowVirtualizer.getVirtualItems().map((vr) => {
              const row = rows[vr.index];
              return (
                <TableRow
                  key={row.id}
                  data-index={vr.index}
                  className="absolute w-full"
                  style={{ transform: `translateY(${vr.start}px)` }}
                >
                  {row.getVisibleCells().map((cell) => (
                    <TableCell key={cell.id} style={{ width: cell.column.getSize() }}>
                      {flexRender(cell.column.columnDef.cell, cell.getContext())}
                    </TableCell>
                  ))}
                </TableRow>
              );
            })}
          </TableBody>
        </Table>
      </div>
    </Card>
  );
}
```

* **가상 행** 구현은 TanStack Table 공식 **Virtualized Rows** 예시 패턴(상단 sticky header + `useVirtualizer`)을 따릅니다. 수만 행에서도 **매끄러운 스크롤**과 **낮은 메모리 사용량**을 보장합니다. 동적 높이가 필요한 경우 `measureElement`를 추가하세요. ([TanStack][2])
* TanStack Table은 **헤드리스**이므로 shadcn/ui `<Table/>` 스타일과 손쉽게 결합됩니다. 필요 시 정렬/필터/페이지네이션을 shadcn의 **Data Table** 가이드처럼 구성할 수 있습니다. ([Shadcn UI][14])
* 가상화 훅은 `@tanstack/react-virtual`의 `useVirtualizer`를 사용합니다(공식 문서/예시 참고). ([TanStack][15])

---

## 7) 우측 패널(인스펙터/에디터)

**`src/components/app/inspector-panel.tsx`**

```tsx
import { ScrollArea } from "@/components/ui/scroll-area";
import { Card } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { Button } from "@/components/ui/button";

export function InspectorPanel() {
  return (
    <ScrollArea className="h-full">
      <div className="flex flex-col gap-3 p-3">
        <Card className="space-y-2 p-3">
          <div className="text-sm font-semibold">세부 정보</div>
          <div className="text-xs text-muted-foreground">행을 선택하면 여기에 편집할 수 있습니다.</div>
          <Label>원문</Label>
          <Textarea rows={6} readOnly placeholder="선택한 원문이 표시됩니다." />
          <Label>번역문</Label>
          <Textarea rows={8} placeholder="여기에 번역문을 편집..." />
          <div className="flex gap-2">
            <Button size="sm">적용</Button>
            <Button size="sm" variant="secondary">되돌리기</Button>
          </div>
        </Card>
      </div>
    </ScrollArea>
  );
}
```

---

## 8) 진행률/토스트/피드백

* **Progress**: 번역 배치/큐 소비율을 상단 또는 테이블 하단에 보여줍니다. ([Shadcn UI][16])
* **Sonner(Toast)**: 항목 저장, 오류, 레이트 리미트 등 즉각 피드백을 띄웁니다. ([Shadcn UI][17])

```tsx
import { Progress } from "@/components/ui/progress";
import { toast } from "sonner";

// 예: 진행률 업데이트
<Progress value={progress} />

// 예: 번역 완료 토스트
toast("번역이 완료되었습니다.", { description: "3,412개 항목 처리" });
```

---

## 9) 컴포넌트 토큰/접근성/성능 팁

* **Radix Primitives** 기반 컴포넌트라 **WAI-ARIA 키보드 내비게이션/포커스 관리**가 내장되어 접근성 기본기를 확보합니다. ([radix-ui.com][3])
* **가상 스크롤 튜닝**: `estimateSize`를 실제 행 평균 높이에 가깝게 맞추고 `overscan`(6\~12)을 조정하세요. TanStack Table의 **Virtualization Guide**가 전략을 잘 정리합니다. ([TanStack][13])
* **열/행 수천\~수만**: TanStack의 공식 **virtualized rows 예시**와 **@tanstack/react-virtual 문서** 패턴을 그대로 따르세요. 필요 시 컬럼 가상화도 있으며, 실험 예시가 제공됩니다. ([TanStack][2])
* **다크 모드**: shadcn/ui **Vite 다크 모드 가이드**의 `ThemeProvider`/`ModeToggle` 조합을 그대로 사용했습니다. Tailwind v4의 `@custom-variant dark`로 수동 토글을 보강합니다. ([Shadcn UI][5], [Tailwind CSS][8])
* **아이콘**: Lucide는 **ESM + 트리 셰이킹**으로 번들 최적화에 유리합니다. ([Lucide][4])

---

## 10) 적용 순서 체크리스트

1. `vite.config.ts`에 `tailwindcss()` 플러그인과 `alias` 설정 추가. ([Shadcn UI][1])
2. `src/index.css`에 `@import "tailwindcss";`와 **테마 변수/다크 변형** 추가. ([Shadcn UI][1], [Tailwind CSS][8])
3. `npx shadcn@latest init` → `components.json` 생성 후, `npx shadcn@latest add ...`로 필요한 컴포넌트 설치. ([Shadcn UI][1])
4. `ThemeProvider`/`ModeToggle` 추가해 **system/light/dark** 토글 구현. ([Shadcn UI][5])
5. `Sidebar + Menubar + Resizable`로 3-패널 앱 셸 구성. ([Shadcn UI][9])
6. 중앙 그리드에 **TanStack Table + Virtualizer** 연결(상단 sticky header). ([TanStack][2])
7. 진행률(Progress) & 토스트(Sonner)로 피드백 UX 보강. ([Shadcn UI][16])

---

## 11) 왜 이 조합이 좋은가

* **현대적 비주얼 + 윈도우 11 톤**: shadcn/ui 테마 변수(OKLCH)와 Sidebar/Menubar 등 구성요소로 **정돈된 데스크톱 도구** UX를 빠르게 확보. ([Shadcn UI][7])
* **대용량/실시간 갱신에 강함**: TanStack Table은 헤드리스 구조로 **가상 스크롤**을 권장 패턴으로 결합할 수 있고, 공식 가이드/예시가 잘 갖춰져 있습니다. ([TanStack][13])
* **접근성 기본기 충족**: Radix Primitives 기반이라 키보드 탐색/포커스/ARIA가 검증되어 있습니다. ([radix-ui.com][18])
* **생태계 연동**: Lucide 아이콘, Sonner 토스트, shadcn Data Table 가이드 등 **실전 예시**가 풍부합니다. ([Lucide][4], [Shadcn UI][17])

---

### 다음 단계(옵션)

* **Inline 편집**(셀 더블클릭 → Textarea/Editor): Data Table 가이드 패턴으로 쉽게 확장. ([Shadcn UI][12])
* **열 고정/리사이즈/정렬**: TanStack Table 기능 가이드 활용. ([TanStack][2])
* **무한 스크롤**: Virtualizer의 **Infinite Scrolling** 예시 참고. ([TanStack][19])

---

필요하시면 위 스켈레톤에 **실제 XML 파서/번역 파이프라인 바인딩**과 **행 선택 → 우측 편집 반영/즉시 저장**까지 동작하는 미니 앱 샘플을 바로 만들어 드릴게요.

[1]: https://ui.shadcn.com/docs/installation/vite "Vite - shadcn/ui"
[2]: https://tanstack.com/table/v8/docs/framework/react/examples/virtualized-rows "React TanStack Table Virtualized Rows Example | TanStack Table Docs"
[3]: https://www.radix-ui.com/primitives?utm_source=chatgpt.com "Radix Primitives"
[4]: https://lucide.dev/guide/packages/lucide-react?utm_source=chatgpt.com "Lucide React"
[5]: https://ui.shadcn.com/docs/dark-mode/vite "Vite - shadcn/ui"
[6]: https://www.npmjs.com/package/lucide-react?utm_source=chatgpt.com "lucide-react"
[7]: https://ui.shadcn.com/docs/theming "Theming - shadcn/ui"
[8]: https://tailwindcss.com/docs/dark-mode?utm_source=chatgpt.com "Dark mode - Core concepts"
[9]: https://ui.shadcn.com/docs/components/sidebar "Sidebar - shadcn/ui"
[10]: https://ui.shadcn.com/docs/components/menubar "Menubar - shadcn/ui"
[11]: https://ui.shadcn.com/docs/components/scroll-area "Scroll-area - shadcn/ui"
[12]: https://ui.shadcn.com/docs/components/data-table "Data Table - shadcn/ui"
[13]: https://tanstack.com/table/v8/docs/guide/virtualization?utm_source=chatgpt.com "Virtualization Guide | TanStack Table Docs"
[14]: https://ui.shadcn.com/docs/components/table "Table - shadcn/ui"
[15]: https://tanstack.com/virtual/latest/docs/framework/react/react-virtual?utm_source=chatgpt.com "React Virtual | TanStack Virtual React Docs"
[16]: https://ui.shadcn.com/docs/components/progress "Progress - shadcn/ui"
[17]: https://ui.shadcn.com/docs/components/sonner "Sonner - shadcn/ui"
[18]: https://www.radix-ui.com/primitives/docs/overview/accessibility?utm_source=chatgpt.com "Accessibility – Radix Primitives"
[19]: https://tanstack.com/table/v8/docs/framework/react/examples/virtualized-rows?utm_source=chatgpt.com "React TanStack Table Virtualized Rows Example"
